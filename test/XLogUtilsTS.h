#include "XLogUtils.h"

#include <cxxtest/TestSuite.h>

class LogUtilsTS: public CxxTest::TestSuite
{
public:

	void testLogDump(void)
	{
		//Log l("Dump Test");
		//char buffer[100];
		//LOG(l, LogDump(buffer, sizeof(buffer)));
	}

};
